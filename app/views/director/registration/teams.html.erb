<% content_for :head_title do %>Teams<% end %>
<% content_for :top_nav do %><%= render :partial => 'shared/top_nav',:locals => {:param => "myracemenu"}%><% end %>
<% content_for :top_area do %><%= render :partial => 'director/shared/top_nav', :locals => {:step => "3"}%><% end %>
<% content_for :extra_head do %>

	<%= javascript_include_tag "default-values" %>
<% end %>
<table width="900" border="0" cellspacing="0" cellpadding="0">
<tr>
<td width="6">&nbsp;</td>
<td width="774" align="center" valign="bottom" class="tableHEAD"><p>Teams</p></td>
<td width="120" class="sidebarHeaderText">Featured Events </td>
</tr>
<tr>
<td>&nbsp;</td>
<td class="content_bg">
	<div id="director_table_holder">
	<table width="100%" cellspacing="0">

		<tr class="director_header_row">
				<td width="20px"></td>
				<td><%= show_sort_text("Reg. Date", "created_at") %> <%=show_sort_image("created_at")%></td>
        <td><%= show_sort_text("Team Name", "name") %> <%=show_sort_image("name")%></td>
				<td><%= show_sort_text("Cpt First Name", "cpt_first_name") %> <%=show_sort_image("cpt_first_name")%></td>
				<td><%= show_sort_text("Cpt Last Name", "cpt_last_name") %> <%=show_sort_image("cpt_last_name")%></td>
        <td><%= show_sort_text("Approved", "approved") %> <%=show_sort_image("approved")%></td>
        <td>Members</td>
				<td align="right"></td>
				
			</tr>
		<tr class="director_row1">
			<td></td>
			<td colspan="7"><%= will_paginate(@teams, :params => {:search_req => @search_req, :sort => params[:sort]}) %></td>
		</tr>
		<% @teams.each do | team | %>
        <tr class="<%=cycle('director_row1', 'director_row2') %>">
            <td width="20px"></td>
			      <td><%= team.created_at.strftime('%m/%d/%Y') %></td>
            <td><%= team.name %></td>
            <td><%= team.cpt_first_name %></td>
            <td><%= team.cpt_last_name %></td>
            <td><%= team.approved %></td>
            <td><%=link_to team.team_entries.size, :controller => "registration", :action => "team_members", :id => team.id%></td>
            <td align="right"><%=link_to cog_button_image, :controller => "registration", :action => "edit_team", :id => team.id%></td>
            
        </tr>

		<% end %>
		<tr class="director_row1">
			<td></td>
			<td colspan="7"><%= will_paginate(@teams, :params => {:search_req => @search_req, :sort => params[:sort]}) %></td>
		</tr>

	</table>
	</div>

</td>
<td width="120" rowspan="2" align="left" valign="top" id="ads"><%= render :partial => 'shared/ads'%></td>
<td style="padding-right:10px;"></td>
</tr>
<tr>
<td>&nbsp;</td>
<td id="tableBOTTOMshadow">&nbsp;</td>
</tr>
<%= render :partial => 'shared/footer'%>
</table>


